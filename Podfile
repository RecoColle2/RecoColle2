# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RecoColle2' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecoColle2
pod "Alamofire"
pod "Kanna"
end

target 'Share' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Share
  pod "Alamofire"
  pod "Kanna"
end
